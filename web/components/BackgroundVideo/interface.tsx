export interface Props {
  videoFile: {
    video2: {
      url: string;
    };
    video: {
      url: string;
    };
  };
  selectVideoType: string;
  videoUrl: string;
  fullScreenVideoComp?: boolean;
}
